# Production Environment Variables Template
# Copy this file to .env.production and fill in your production values

# Database Configuration
# PostgreSQL connection string with SSL enabled
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public&sslmode=require
DATABASE_URL="postgresql://user:password@host:5432/expense_tracker?schema=public&sslmode=require"

# Node Environment (must be 'production')
NODE_ENV="production"

# Next.js Configuration
# Base URL for your production deployment (used for absolute URLs)
NEXT_PUBLIC_APP_URL="https://your-domain.com"

# Security
# Generate a secure random string for session encryption (if using authentication in future)
# SECRET_KEY="your-secure-random-string-here"

# Monitoring & Analytics (Optional)
# SENTRY_DSN="your-sentry-dsn"
# NEXT_PUBLIC_GA_ID="your-google-analytics-id"
# VERCEL_ANALYTICS_ID="your-vercel-analytics-id"

# Notes:
# - Never commit this file to version control
# - Use environment-specific values for each deployment
# - Ensure DATABASE_URL uses SSL in production (?sslmode=require)
# - All NEXT_PUBLIC_* variables are exposed to the browser
# - Keep sensitive credentials in server-side variables only
